# Leano · 虚拟小手机（v1.3）

一台仿 iPhone 16 操作体验的黑白 ins 风「虚拟小手机」，纯前端、断网可完整运行。

## 手机上怎么打开
1. **单文件版（最简单）**：只把 `Leano-单文件版.html` 传到手机，用 Edge / 浏览器打开。
2. **电脑当服务器（同一 WiFi）**：双击 `tools\启动本地服务.cmd`，手机访问提示的网址。
3. **像 App 安装（先联网托管）**：用 `Leano-网页版.zip` 上传到 HTTPS 静态托管（方法见 `GitHub-上传步骤.md`），手机 Edge 打开后「添加到主屏幕 / 安装应用」。
4. **电脑本地**：双击 `index.html`。

> 改代码后请双击 `tools\生成单文件版.cmd` 重新生成单文件版。
> 数据保存在本机浏览器，刷新不丢；「设置 → 清空数据」可重置。

## 手势
锁屏上滑解锁 · 主屏左右翻页 · 顶部左/右＝通知/控制中心 · 底部上滑回主屏 · 底部按住 0.4 秒＝App 切换器 · Esc＝返回（电脑）。

## 自定义图片（v1.2 新增）
打开「设置 → 自定义图片」可把本机图片上传替换：
- 主屏壁纸 / 锁屏壁纸
- 顶部组件图、第 2 页三张拼贴图
- 我的头像、恋人头像（Eric）
- 全部 App 图标（点一下即可换成你的图）
- 图片会自动压缩后保存在本机；可单独重置或全部恢复默认。

## 主题与模式（v1.3）
- 设置 → 外观主题：整台手机白/黑切换，默认白色。
- 设置 → 运行模式：人机恋 ⇄ airp（18+ AI 成人向角色扮演）。airp 有独立角色库与专属聊天皮肤（角色状态卡、语音按钮占位），首次进入需 18+ 二次确认；本期不接真实 AI，回复沿用占位引擎。

## 角色与 AI
「微信」聊天；「角色扮演 / 情侣空间 / 聊天 ＋」进角色中心：广场 / 接key / 自建 / 我的。
演示解锁码：`LEANO-GU`（顾屿）、`LEANO-JIANG`（江野）。可自建角色、绑定自己的 key（仅存本机）。
第一版为预设回复，接口已预留，二期接真实 AI。

## 版本记录
- v1.3：新增黑白主题（默认白）与人机恋/airp 双模式前端、airp 独立角色库与聊天皮肤。
- v1.2：顶部时钟改为 iOS 原版数字时钟组件；组件改为玻璃质感；支持自定义上传壁纸/头像/组件图/App 图标。
- v1.1：修复手机端上滑；锁屏去掉“暂无通知”；锁屏圆钮玻璃化；新增 PWA 可安装、离线缓存、图标、局域网服务器与发布包。
- v1.0：初版（18+ 确认、锁屏、两页主屏、系统手势、角色系统、7 个核心 App）。

## 说明
- 首次使用有 18+ 自述确认；仅供 18 岁以上用户使用。
- 参考图在 `参考图/`（不上传 GitHub）；测试截图在 `预览/`。